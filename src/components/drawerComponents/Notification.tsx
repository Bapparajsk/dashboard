import {ComponentWrapper} from "@/components/drawerComponents/Wrapper";

export const Notification = () => {
    return (
        <ComponentWrapper>
            hello
        </ComponentWrapper>
    );
};
